---
agent: 'agent'
description: 'Fix CI for Docker issues by running the CI pipeline locally and analyzing the results.'
---

To fix CI issues, you can run the CI pipeline locally via `make ci-test-docker` to reproduce the problem and analyze the results.
