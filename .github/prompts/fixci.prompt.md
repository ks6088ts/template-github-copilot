---
agent: 'agent'
description: 'Fix CI issues by running the CI pipeline locally and analyzing the results.'
---

To fix CI issues, you can run the CI pipeline locally via `make ci-test` to reproduce the problem and analyze the results.
